project_name: rhcsa-tui
version: 2
before:
  hooks:
    - go mod download
builds:
  - id: rhcsa-tui
    main: ./cmd/rhcsa-tui
    binary: rhcsa-tui
    env:
      - CGO_ENABLED=0
    goos:
      - windows
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
archives:
  - id: default
    format_overrides:
      - goos: windows
        format: zip
checksum:
  algorithm: sha256
snapshot:
  name_template: "{{ .Tag }}-next"
release:
  github:
    owner: "{{ .Env.GH_OWNER }}"
    name: "{{ .Env.GH_REPO }}"
  draft: true
  prerelease: auto
