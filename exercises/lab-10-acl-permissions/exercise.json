{
  "id": "lab-10-acl-permissions",
  "title": "Lab 10: ACL And Permissions",
  "description": "Apply fine grained access with POSIX ACLs.",
  "time_limit_minutes": 25,
  "objective_tags": [
    "selinux-and-default-perms"
  ],
  "requires_servervm": false,
  "source_manifest": "scenarios/labs/lab-10-acl-permissions/scenario.json",
  "paths": {
    "prompt": "exercises/lab-10-acl-permissions/prompt.md",
    "hint": "exercises/lab-10-acl-permissions/hint.md",
    "check": "exercises/lab-10-acl-permissions/check.sh",
    "solution": "exercises/lab-10-acl-permissions/solution.md",
    "metadata": "exercises/lab-10-acl-permissions/exercise.json"
  },
  "tasks": [
    {
      "index": 1,
      "title": "Copy /etc/fstab to /var/tmp/fstab-acl",
      "points": 10,
      "system": "clientvm",
      "prompt": "Copy /etc/fstab to /var/tmp/fstab-acl.",
      "solution_commands": [
        "id natacl || useradd -m natacl",
        "id haracl || useradd -m haracl",
        "cp /etc/fstab /var/tmp/fstab-acl",
        "chown root:root /var/tmp/fstab-acl",
        "chmod 644 /var/tmp/fstab-acl"
      ]
    },
    {
      "index": 2,
      "title": "Set owner and group to root:root, remove all",
      "points": 10,
      "system": "clientvm",
      "prompt": "Set owner and group to root:root, remove all execute bits, give natacl read-write, deny haracl all access, and allow others read only.",
      "solution_commands": [
        "setfacl -m u:natacl:rw- /var/tmp/fstab-acl",
        "setfacl -m u:haracl:--- /var/tmp/fstab-acl",
        "getfacl /var/tmp/fstab-acl"
      ]
    }
  ],
  "hints": [
    "Create the users natacl and haracl if they are missing.",
    "Use setfacl for the named user entries."
  ],
  "checks": [
    {
      "index": 1,
      "target": "clientvm",
      "original_command": "stat -c '%U:%G %a' /var/tmp/fstab-acl | grep -qx 'root:root 644'",
      "command": "stat -c '%U:%G %a' /var/tmp/fstab-acl | grep -qx 'root:root 644'"
    },
    {
      "index": 2,
      "target": "clientvm",
      "original_command": "getfacl -cp /var/tmp/fstab-acl | grep -qx 'user:natacl:rw-' && getfacl -cp /var/tmp/fstab-acl | grep -qx 'user:haracl:---' && getfacl -cp /var/tmp/fstab-acl | grep -qx 'other::r--'",
      "command": "getfacl -cp /var/tmp/fstab-acl | grep -qx 'user:natacl:rw-' && getfacl -cp /var/tmp/fstab-acl | grep -qx 'user:haracl:---' && getfacl -cp /var/tmp/fstab-acl | grep -qx 'other::r--'"
    }
  ]
}
