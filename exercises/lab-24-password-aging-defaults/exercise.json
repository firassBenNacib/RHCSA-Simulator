{
  "id": "lab-24-password-aging-defaults",
  "title": "Lab 24: Password Aging Defaults",
  "description": "Configure default password aging for newly created local users through login.defs.",
  "time_limit_minutes": 30,
  "objective_tags": [
    "users-sudo-ssh"
  ],
  "requires_servervm": false,
  "source_manifest": "scenarios/labs/lab-24-password-aging-defaults/scenario.json",
  "paths": {
    "prompt": "exercises/lab-24-password-aging-defaults/prompt.md",
    "hint": "exercises/lab-24-password-aging-defaults/hint.md",
    "check": "exercises/lab-24-password-aging-defaults/check.sh",
    "solution": "exercises/lab-24-password-aging-defaults/solution.md",
    "metadata": "exercises/lab-24-password-aging-defaults/exercise.json"
  },
  "tasks": [
    {
      "index": 1,
      "title": "Configure the system defaults for newly created",
      "points": 10,
      "system": "clientvm",
      "prompt": "Configure the system defaults for newly created local users with the following values:\n\nPASS_MAX_DAYS: 45\nPASS_MIN_DAYS: 2\nPASS_WARN_AGE: 10",
      "solution_commands": [
        "vim /etc/login.defs",
        "PASS_MAX_DAYS   45",
        "PASS_MIN_DAYS   2",
        "PASS_WARN_AGE   10"
      ]
    },
    {
      "index": 2,
      "title": "Create the user drift24, set its password to",
      "points": 10,
      "system": "clientvm",
      "prompt": "Create the user drift24, set its password to cinder9, and ensure the user inherits the default password aging policy.",
      "solution_commands": [
        "useradd -m drift24",
        "passwd drift24",
        "# enter: cinder9",
        "chage -l drift24"
      ]
    }
  ],
  "hints": [
    "Edit /etc/login.defs directly.",
    "Use chage -l to verify the new account settings."
  ],
  "checks": [
    {
      "index": 1,
      "target": "clientvm",
      "original_command": "grep -Eq '^[[:space:]]*PASS_MAX_DAYS[[:space:]]+45[[:space:]]*$' /etc/login.defs && grep -Eq '^[[:space:]]*PASS_MIN_DAYS[[:space:]]+2[[:space:]]*$' /etc/login.defs && grep -Eq '^[[:space:]]*PASS_WARN_AGE[[:space:]]+10[[:space:]]*$' /etc/login.defs",
      "command": "grep -Eq '^[[:space:]]*PASS_MAX_DAYS[[:space:]]+45[[:space:]]*$' /etc/login.defs && grep -Eq '^[[:space:]]*PASS_MIN_DAYS[[:space:]]+2[[:space:]]*$' /etc/login.defs && grep -Eq '^[[:space:]]*PASS_WARN_AGE[[:space:]]+10[[:space:]]*$' /etc/login.defs"
    },
    {
      "index": 2,
      "target": "clientvm",
      "original_command": "chage -l drift24 | grep -Eq 'Minimum number of days between password change[^0-9]*2$' && chage -l drift24 | grep -Eq 'Maximum number of days between password change[^0-9]*45$' && chage -l drift24 | grep -Eq 'Number of days of warning before password expires[^0-9]*10$'",
      "command": "chage -l drift24 | grep -Eq 'Minimum number of days between password change[^0-9]*2$' && chage -l drift24 | grep -Eq 'Maximum number of days between password change[^0-9]*45$' && chage -l drift24 | grep -Eq 'Number of days of warning before password expires[^0-9]*10$'"
    }
  ]
}
