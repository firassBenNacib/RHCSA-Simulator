{
  "id": "lab-43-package-install-remove",
  "title": "Lab 43: Package Install And Remove",
  "description": "Install packages from the prepared local repositories and remove the one that is no longer needed",
  "objective_tags": [
    "repositories-and-packages"
  ],
  "supported_modes": [
    "lab"
  ],
  "time_limit_minutes": 25,
  "vm_scripts": {
    "clientvm": "scripts/clientvm.sh"
  },
  "flags": {
    "password_recovery": false,
    "requires_servervm": false
  },
  "content": {
    "lab": {
      "tasks": [
        "Use the prepared local repositories on clientvm to install the packages tree and dos2unix.",
        "Remove dos2unix and leave tree installed."
      ],
      "hints": [
        "The repositories are already defined for this lab. Focus on package management."
      ],
      "checks": [
        "rpm -q tree",
        "! rpm -q dos2unix"
      ],
      "solution_outline": [],
      "task_titles": [],
      "task_points": [],
      "solution_commands": [
        [
          "dnf install -y tree dos2unix"
        ],
        [
          "dnf remove -y dos2unix",
          "rpm -q tree"
        ]
      ]
    }
  }
}
