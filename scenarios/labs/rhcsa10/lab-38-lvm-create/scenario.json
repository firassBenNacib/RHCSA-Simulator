{
  "id": "lab-38-lvm-create",
  "title": "RHCSA 10 Lab 38: LVM Create",
  "description": "Create and mount a logical volume",
  "objective_tags": [
    "storage-lvm"
  ],
  "supported_modes": [
    "lab"
  ],
  "time_limit_minutes": 40,
  "tracks": [
    "rhcsa10"
  ],
  "rhel_major": 10,
  "flags": {
    "password_recovery": false,
    "requires_server": false
  },
  "content": {
    "lab": {
      "tasks": [
        "On client, create physical volume /dev/sdb.",
        "On client, create volume group vg10.",
        "On client, using volume group vg10 on /dev/sdb, create a 384 MiB logical volume lvdata, format it with XFS, and mount it persistently at /mnt/lvdata10."
      ],
      "hints": [
        "Use pvcreate, vgcreate, and lvcreate.",
        "Add the logical volume to /etc/fstab."
      ],
      "checks": [
        "pvs /dev/sdb >/dev/null",
        "vgs vg10 >/dev/null",
        "lv_uuid=\"$(blkid -s UUID -o value /dev/vg10/lvdata)\"; lvs /dev/vg10/lvdata >/dev/null && findmnt -no TARGET /mnt/lvdata10 | grep -qx /mnt/lvdata10 && awk -v dev=\"/dev/vg10/lvdata\" -v mapper=\"/dev/mapper/vg10-lvdata\" -v uuid=\"$lv_uuid\" '$1 !~ /^#/ && $2 == \"/mnt/lvdata10\" && $3 == \"xfs\" && ($1 == dev || $1 == mapper || $1 == \"UUID=\" uuid || $1 == \"/dev/disk/by-uuid/\" uuid) {found=1} END {exit !found}' /etc/fstab"
      ],
      "solution_outline": [
        "Make the requested configuration persistent and verify it before finishing."
      ],
      "task_titles": [
        "Create physical volume /dev/sdb",
        "Create volume group",
        "Configure LVM storage"
      ],
      "task_points": [
        10,
        10,
        10
      ],
      "solution_commands": [
        [
          "wipefs -a /dev/sdb >/dev/null 2>&1 || true",
          "sgdisk --zap-all /dev/sdb >/dev/null 2>&1 || true",
          "pvcreate -ff -y /dev/sdb"
        ],
        [
          "vgcreate vg10 /dev/sdb"
        ],
        [
          "lvcreate -L 384M -n lvdata vg10",
          "mkfs.xfs -f /dev/vg10/lvdata",
          "mkdir -p /mnt/lvdata10",
          "uuid=$(blkid -s UUID -o value /dev/vg10/lvdata)",
          "test -n \"$uuid\" && echo \"UUID=$uuid /mnt/lvdata10 xfs defaults 0 0\" >> /etc/fstab",
          "mount -a"
        ]
      ],
      "task_targets": [
        "client",
        "client",
        "client"
      ],
      "solution_targets": [
        "client",
        "client",
        "client"
      ],
      "check_targets": [
        "client",
        "client",
        "client"
      ]
    }
  },
  "vm_scripts": {
    "client": "scripts/client.sh"
  },
  "scope": "client"
}
