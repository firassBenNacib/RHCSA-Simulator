{
  "id": "lab-18-ssh-key-auth",
  "title": "RHCSA 10 Lab 18: SSH Key Authentication",
  "description": "Configure key-based SSH authentication",
  "objective_tags": [
    "users-sudo-ssh"
  ],
  "supported_modes": [
    "lab"
  ],
  "time_limit_minutes": 30,
  "tracks": [
    "rhcsa10"
  ],
  "rhel_major": 10,
  "flags": {
    "password_recovery": false,
    "requires_server": true
  },
  "content": {
    "lab": {
      "tasks": [
        "On client, create user key10 and set password cinder9.",
        "On server, create user key10 and set password cinder9.",
        "On client, generate an ED25519 SSH key pair for key10 with no passphrase.",
        "On client, configure key-based SSH authentication so key10 can log in to key10@server without a password prompt."
      ],
      "hints": [
        "The .ssh directory should be 700.",
        "authorized_keys should be 600 and owned by the user."
      ],
      "checks": [
        "getent passwd key10 >/dev/null",
        "# server getent passwd key10 >/dev/null",
        "test -f /home/key10/.ssh/id_ed25519.pub && stat -c '%U:%a' /home/key10/.ssh | grep -qx 'key10:700'",
        "runuser -l key10 -c 'ssh -o BatchMode=yes -o NumberOfPasswordPrompts=0 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null key10@server true'"
      ],
      "solution_outline": [
        "Make the requested configuration persistent and verify it before finishing."
      ],
      "task_titles": [
        "Create user key10 and set password cinder9",
        "Create user key10 and set password cinder9",
        "Generate an ED25519 SSH key pair for key10 with no passphrase",
        "Configure key-based SSH authentication so key10 can log in to key10@serv"
      ],
      "task_points": [
        10,
        10,
        10,
        10
      ],
      "solution_commands": [
        [
          "useradd -m key10",
          "echo 'key10:cinder9' | chpasswd"
        ],
        [
          "# On server",
          "useradd -m key10",
          "echo 'key10:cinder9' | chpasswd"
        ],
        [
          "su - key10",
          "ssh-keygen -t ed25519 -N \"\" -f ~/.ssh/id_ed25519"
        ],
        [
          "su - key10",
          "ssh-copy-id -i ~/.ssh/id_ed25519.pub key10@server"
        ]
      ],
      "task_targets": [
        "client",
        "server",
        "client",
        "both"
      ],
      "solution_targets": [
        "client",
        "server",
        "client",
        "both"
      ],
      "check_targets": [
        "client",
        "server",
        "client",
        "client"
      ]
    }
  },
  "vm_scripts": {
    "client": "scripts/client.sh",
    "server": "scripts/server.sh"
  },
  "scope": "client-server"
}
