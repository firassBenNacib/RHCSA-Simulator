{
  "id": "lab-45-restorecon-httpd",
  "title": "Lab 45: Restore Default SELinux Context",
  "description": "Restore the default SELinux context on existing web content without disabling SELinux",
  "objective_tags": [
    "selinux-and-default-perms"
  ],
  "supported_modes": [
    "lab"
  ],
  "time_limit_minutes": 25,
  "vm_scripts": {
    "clientvm": "scripts/clientvm.sh"
  },
  "flags": {
    "password_recovery": false,
    "requires_servervm": false
  },
  "content": {
    "lab": {
      "tasks": [
        "On clientvm, the file /var/www/html/index45.html has the wrong SELinux context. Restore the default context.",
        "Ensure the httpd service is enabled and running. SELinux must remain enforcing."
      ],
      "hints": [
        "Do not use chcon for the final state."
      ],
      "checks": [
        "getenforce | grep -qx Enforcing",
        "matchpathcon /var/www/html/index45.html | grep -Eq ':httpd_sys_content_t:' && ls -Z /var/www/html/index45.html | grep -Eq ':httpd_sys_content_t:'",
        "systemctl is-enabled httpd | grep -qx enabled && systemctl is-active httpd | grep -qx active"
      ],
      "solution_outline": [],
      "task_titles": [
        "the file /var/www/html/index45.html has the wrong...",
        "Ensure the httpd service is enabled and running...."
      ],
      "task_points": [
        10,
        10
      ],
      "solution_commands": [
        [
          "restorecon -v /var/www/html/index45.html"
        ],
        [
          "systemctl enable --now httpd",
          "getenforce",
          "ls -Z /var/www/html/index45.html"
        ]
      ]
    }
  }
}
