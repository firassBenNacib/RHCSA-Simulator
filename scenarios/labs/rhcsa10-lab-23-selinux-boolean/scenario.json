{
  "id": "rhcsa10-lab-23-selinux-boolean",
  "title": "RHCSA 10 Lab 23: SELinux Boolean",
  "description": "Set SELinux booleans persistently",
  "objective_tags": [
    "selinux-and-default-perms"
  ],
  "supported_modes": [
    "lab"
  ],
  "time_limit_minutes": 15,
  "tracks": [
    "rhcsa10"
  ],
  "rhel_major": 10,
  "flags": {
    "password_recovery": false,
    "requires_server": false
  },
  "content": {
    "lab": {
      "tasks": [
        "Enable the httpd_can_network_connect SELinux boolean.",
        "Make the boolean persistent.",
        "Verify the boolean state."
      ],
      "hints": [
        "Use setsebool -P.",
        "Use getsebool for verification."
      ],
      "checks": [
        "getsebool httpd_can_network_connect | grep -Eq -- '--> on$'",
        "semanage boolean -l | grep -E '^httpd_can_network_connect\\s' | grep -q on",
        "getsebool httpd_can_network_connect >/dev/null"
      ],
      "solution_outline": [
        "Make the requested configuration persistent and verify it before finishing."
      ],
      "task_titles": [
        "Enable the httpd_can_network_connect SELinux boolean",
        "Make the boolean persistent",
        "Verify the boolean state"
      ],
      "task_points": [
        10,
        10,
        10
      ],
      "solution_commands": [
        [
          "setsebool -P httpd_can_network_connect on"
        ],
        [
          "getsebool httpd_can_network_connect"
        ],
        [
          "semanage boolean -l | grep httpd_can_network_connect"
        ]
      ]
    }
  }
}
