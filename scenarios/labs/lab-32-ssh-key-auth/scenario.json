{
  "id": "lab-32-ssh-key-auth",
  "title": "Lab 32: SSH Key Authentication",
  "description": "Configure passwordless SSH login from clientvm to servervm using a key pair",
  "objective_tags": [
    "users-sudo-ssh"
  ],
  "supported_modes": [
    "lab"
  ],
  "time_limit_minutes": 35,
  "vm_scripts": {
    "clientvm": "scripts/clientvm.sh",
    "servervm": "scripts/servervm.sh"
  },
  "flags": {
    "password_recovery": false,
    "requires_servervm": true
  },
  "content": {
    "lab": {
      "tasks": [
        "Create user relay32 on clientvm and user vault32 on servervm. Set the password of both users to cinder9.",
        "Configure key-based SSH authentication so that user relay32 on clientvm can log in to vault32@servervm without a password prompt.",
        "Do not disable PasswordAuthentication globally for this task."
      ],
      "hints": [
        "Generate the key as the source user.",
        "Populate authorized_keys for the target user."
      ],
      "checks": [
        "runuser -l relay32 -c 'ssh -o StrictHostKeyChecking=no -o BatchMode=yes vault32@servervm true'"
      ],
      "solution_outline": [
        "Use persistent configuration only."
      ],
      "task_titles": [
        "Create user relay32 on clientvm and user vault32 on...",
        "Configure key-based SSH authentication so that user...",
        "Do not disable PasswordAuthentication globally for..."
      ],
      "task_points": [
        10,
        10,
        10
      ],
      "solution_commands": [
        [
          "useradd -m relay32",
          "passwd relay32",
          "# enter: cinder9",
          "# on servervm",
          "useradd -m vault32",
          "passwd vault32",
          "# enter: cinder9"
        ],
        [
          "runuser -l relay32 -c \"ssh-keygen -t rsa -N \"\" -f ~/.ssh/id_rsa\"",
          "runuser -l relay32 -c \"ssh-copy-id vault32@servervm\""
        ],
        [
          "runuser -l relay32 -c \"ssh -o StrictHostKeyChecking=no vault32@servervm true\""
        ]
      ]
    }
  }
}
