{
  "id": "lab-33-grub-kernel-arg",
  "title": "Lab 33: Bootloader Kernel Argument",
  "description": "Modify the system bootloader so every installed kernel boots with the required persistent argument",
  "objective_tags": [
    "boot-and-recovery"
  ],
  "supported_modes": [
    "lab"
  ],
  "time_limit_minutes": 20,
  "tracks": [
    "rhcsa9"
  ],
  "rhel_major": 9,
  "vm_scripts": {
    "client": "scripts/client.sh"
  },
  "flags": {
    "password_recovery": false,
    "requires_server": false
  },
  "content": {
    "lab": {
      "tasks": [
        "On client, configure the bootloader on client so that every installed kernel boots persistently with the kernel argument audit_backlog_limit=8192 without manual GRUB editing during startup."
      ],
      "hints": [
        "Use a persistent bootloader tool rather than a one-time edit at the GRUB menu.",
        "Verify the configured kernel arguments for all installed kernels."
      ],
      "checks": [
        "grubby --info=ALL | grep -E \"^args=\" | grep -q \"audit_backlog_limit=8192\""
      ],
      "solution_outline": [
        "Use grubby to update all installed kernels."
      ],
      "task_titles": [
        "Configure the persistent kernel argument"
      ],
      "task_points": [
        10
      ],
      "solution_commands": [
        [
          "grubby --update-kernel=ALL --args=\"audit_backlog_limit=8192\""
        ]
      ],
      "task_targets": [
        "client"
      ],
      "solution_targets": [
        "client"
      ],
      "check_targets": [
        "client"
      ]
    }
  },
  "scope": "client"
}
