{
  "id": "lab-02-root-recovery",
  "title": "Lab 02: Root Password Recovery",
  "description": "Recover root access through the bootloader and restore normal access on client",
  "objective_tags": [
    "boot-and-recovery"
  ],
  "supported_modes": [
    "lab"
  ],
  "time_limit_minutes": 40,
  "tracks": [
    "rhcsa9"
  ],
  "rhel_major": 9,
  "vm_scripts": {
    "client": "scripts/client.sh"
  },
  "flags": {
    "password_recovery": true,
    "requires_server": false
  },
  "content": {
    "lab": {
      "tasks": [
        "On client, recover root access on client from the console and set the root password to cinder9.",
        "On client, after the system boots normally, confirm that SELinux relabeling completed and root can log in again.",
        "On client, leave SSH password authentication working for root and admin."
      ],
      "hints": [
        "Use the boot menu edit path with rw init=/bin/bash.",
        "Remember to touch /.autorelabel before starting the normal init process."
      ],
      "checks": [
        "getenforce | grep -qx Enforcing",
        "ls -Zd /root | grep -Eq '(^| )[^ ]+:object_r:admin_home_t:s0($| )'",
        "sshd -T | grep -Eq '^passwordauthentication yes$' && sshd -T | grep -Eq '^permitrootlogin yes$'"
      ],
      "solution_outline": [
        "Use persistent configuration only."
      ],
      "task_titles": [
        "Recover root access on client from the console...",
        "After the system boots normally, confirm that...",
        "Leave SSH password authentication working for root..."
      ],
      "task_points": [
        10,
        10,
        10
      ],
      "solution_commands": [
        [
          "# At the boot menu, edit the selected kernel entry.",
          "# Append rw init=/bin/bash to the linux line and boot with Ctrl+x.",
          "passwd root",
          "# enter: cinder9",
          "touch /.autorelabel",
          "exec /sbin/init"
        ],
        [
          "getenforce",
          "ls -Z /root | head"
        ],
        [
          "vim /etc/ssh/sshd_config",
          "PasswordAuthentication yes",
          "PermitRootLogin yes",
          "systemctl restart sshd"
        ]
      ],
      "task_targets": [
        "client",
        "client",
        "client"
      ],
      "solution_targets": [
        "client",
        "client",
        "client"
      ],
      "check_targets": [
        "client",
        "client",
        "client"
      ]
    }
  },
  "scope": "client"
}
