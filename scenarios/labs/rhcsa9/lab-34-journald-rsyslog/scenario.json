{
  "id": "lab-34-journald-rsyslog",
  "title": "Lab 34: Journald Persistence and Rsyslog",
  "description": "Configure persistent journal storage and a custom rsyslog drop-in for authentication warnings",
  "objective_tags": [
    "processes-logs-tuning",
    "software-scheduling-time"
  ],
  "supported_modes": [
    "lab"
  ],
  "time_limit_minutes": 25,
  "tracks": [
    "rhcsa9"
  ],
  "rhel_major": 9,
  "vm_scripts": {
    "client": "scripts/client.sh"
  },
  "flags": {
    "password_recovery": false,
    "requires_server": false
  },
  "content": {
    "lab": {
      "tasks": [
        "On client, configure journald on client so logs are stored persistently across reboots.",
        "On client, create the drop-in file /etc/rsyslog.d/10-auth34.conf so authpriv messages with priority warning and higher are written to /var/log/auth34.log.",
        "On client, ensure the rsyslog service is active after your changes."
      ],
      "hints": [
        "Persistent journald storage requires the correct directory and a valid [Journal] configuration section.",
        "Reload or restart the affected logging services after you update their configuration."
      ],
      "checks": [
        "files=\"\"; test -d /var/log/journal && systemctl is-active systemd-journald | grep -qx active && files=$(find /etc/systemd -maxdepth 2 \\( -path /etc/systemd/journald.conf -o -path '/etc/systemd/journald.conf.d/*.conf' \\) -type f 2>/dev/null); test -n \"$files\" && awk 'FNR == 1 {in_journal=0} /^[[:space:]]*\\[Journal\\][[:space:]]*($|#)/ {in_journal=1; next} /^[[:space:]]*\\[/ {in_journal=0} in_journal && /^[[:space:]]*Storage[[:space:]]*=[[:space:]]*persistent[[:space:]]*($|#)/ {found=1} END {exit !found}' $files",
        "grep -Eq '^[[:space:]]*authpriv\\.warning[[:space:]]+/var/log/auth34\\.log[[:space:]]*$' /etc/rsyslog.d/10-auth34.conf",
        "systemctl is-active rsyslog | grep -qx active && : > /var/log/auth34.log && logger -p authpriv.warning 'lab34 probe' && sleep 3 && grep -Fq 'lab34 probe' /var/log/auth34.log"
      ],
      "solution_outline": [
        "Configure journald and rsyslog persistently."
      ],
      "task_titles": [
        "Configure journald on client so logs are stored...",
        "Create the drop-in file...",
        "Ensure the rsyslog service is active after your..."
      ],
      "task_points": [
        10,
        10,
        10
      ],
      "solution_commands": [
        [
          "mkdir -p /var/log/journal /etc/systemd/journald.conf.d",
          "cat > /etc/systemd/journald.conf.d/99-rhcsa-persistent.conf <<'EOF'\n[Journal]\nStorage=persistent\nEOF",
          "systemctl restart systemd-journald",
          "journalctl --flush"
        ],
        [
          "vim /etc/rsyslog.d/10-auth34.conf",
          "authpriv.warning    /var/log/auth34.log"
        ],
        [
          "systemctl restart rsyslog",
          "systemctl enable rsyslog"
        ]
      ],
      "task_targets": [
        "client",
        "client",
        "client"
      ],
      "solution_targets": [
        "client",
        "client",
        "client"
      ],
      "check_targets": [
        "client",
        "client",
        "client"
      ]
    }
  },
  "scope": "client"
}
