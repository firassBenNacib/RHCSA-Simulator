{
  "id": "lab-22-container-autostart",
  "title": "Lab 22: Container Autostart with Systemd",
  "description": "Run a rootless container as a persistent user service",
  "objective_tags": [
    "containers"
  ],
  "supported_modes": [
    "lab"
  ],
  "time_limit_minutes": 35,
  "tracks": [
    "rhcsa9"
  ],
  "rhel_major": 9,
  "vm_scripts": {
    "client": "scripts/client.sh"
  },
  "flags": {
    "password_recovery": false,
    "requires_server": false
  },
  "content": {
    "lab": {
      "tasks": [
        "On client, ensure user merin22 exists, set the password to cinder9, and then, as that user, run a container named render22 from localhost/fluxpdf22:latest with /opt/inbox22 mounted to /data/input and /opt/outbox22 mounted to /data/output.",
        "On client, enable lingering for merin22 so the user service can start automatically after reboot.",
        "On client, as user merin22, generate a systemd user unit for that container and enable it."
      ],
      "hints": [
        "Use podman generate systemd --files --new or podman generate systemd --new depending on the environment.",
        "The user service must run without an active login session."
      ],
      "checks": [
        "getent passwd merin22 >/dev/null && getent shadow merin22 | awk -F: 'length($2)>0 && $2 !~ /^(!!?|\\*|LK|NP)$/' >/dev/null && runuser -l merin22 -c 'podman container exists render22'",
        "loginctl show-user merin22 | grep -Eq '^Linger=yes$'",
        "uid=$(id -u merin22); runuser -l merin22 -c \"XDG_RUNTIME_DIR=/run/user/$uid systemctl --user is-enabled container-render22.service\" | grep -qx enabled && runuser -l merin22 -c \"XDG_RUNTIME_DIR=/run/user/$uid systemctl --user is-active container-render22.service\" | grep -qx active"
      ],
      "solution_outline": [
        "Use persistent configuration only."
      ],
      "task_titles": [
        "Ensure merin22 exists and run the container",
        "Enable lingering for merin22",
        "Generate and enable the user service"
      ],
      "task_points": [
        10,
        10,
        10
      ],
      "solution_commands": [
        [
          "id merin22 >/dev/null 2>&1 || useradd -m merin22",
          "passwd merin22",
          "# enter: cinder9",
          "su - merin22",
          "podman run -d --name render22 -v /opt/inbox22:/data/input:Z -v /opt/outbox22:/data/output:Z localhost/fluxpdf22:latest",
          "exit"
        ],
        [
          "loginctl enable-linger merin22",
          "uid=$(id -u merin22)",
          "systemctl start user@$uid.service",
          "for i in $(seq 1 30); do [ -S /run/user/$uid/bus ] && break; sleep 1; done"
        ],
        [
          "runuser -l merin22 -c 'mkdir -p ~/.config/systemd/user'",
          "runuser -l merin22 -c 'cd ~/.config/systemd/user && podman generate systemd --name render22 --files'",
          "runuser -l merin22 -c 'podman kill render22 >/dev/null 2>&1 || true'",
          "runuser -l merin22 -c 'XDG_RUNTIME_DIR=/run/user/$(id -u) DBUS_SESSION_BUS_ADDRESS=unix:path=/run/user/$(id -u)/bus systemctl --user daemon-reload'",
          "runuser -l merin22 -c 'XDG_RUNTIME_DIR=/run/user/$(id -u) DBUS_SESSION_BUS_ADDRESS=unix:path=/run/user/$(id -u)/bus systemctl --user enable --now container-render22.service'"
        ]
      ],
      "task_targets": [
        "client",
        "client",
        "client"
      ],
      "solution_targets": [
        "client",
        "client",
        "client"
      ],
      "check_targets": [
        "client",
        "client",
        "client"
      ]
    }
  },
  "scope": "client"
}
