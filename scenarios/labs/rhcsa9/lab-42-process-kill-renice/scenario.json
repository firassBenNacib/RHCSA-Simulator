{
  "id": "lab-42-process-kill-renice",
  "title": "Lab 42: Process Kill and Renice",
  "description": "Identify a running process, terminate it, and adjust the scheduling priority of another one",
  "objective_tags": [
    "processes-logs-tuning"
  ],
  "supported_modes": [
    "lab"
  ],
  "time_limit_minutes": 25,
  "tracks": [
    "rhcsa9"
  ],
  "rhel_major": 9,
  "vm_scripts": {
    "client": "scripts/client.sh"
  },
  "flags": {
    "password_recovery": false,
    "requires_server": false
  },
  "content": {
    "lab": {
      "tasks": [
        "On client, user worker42 has a CPU-bound process whose PID is stored in /home/worker42/cpu.pid. Terminate that process.",
        "On client, user worker42 also has a long-running sleep process whose PID is stored in /home/worker42/sleep.pid. Change the nice value of that process to 10."
      ],
      "hints": [
        "Use kill or pkill for the first process and renice for the second one."
      ],
      "checks": [
        "[ ! -d \"/proc/$(cat /home/worker42/cpu.pid)\" ]",
        "ps -o ni= -p \"$(cat /home/worker42/sleep.pid)\" | tr -d ' ' | grep -qx 10"
      ],
      "solution_outline": [],
      "task_titles": [
        "Terminate worker42's CPU-bound process",
        "Change the long-running sleep process to nice 10"
      ],
      "task_points": [
        10,
        10
      ],
      "solution_commands": [
        [
          "kill \"$(cat /home/worker42/cpu.pid)\""
        ],
        [
          "renice 10 -p \"$(cat /home/worker42/sleep.pid)\""
        ]
      ],
      "task_targets": [
        "client",
        "client"
      ],
      "solution_targets": [
        "client",
        "client"
      ],
      "check_targets": [
        "client",
        "client"
      ]
    }
  },
  "scope": "client"
}
