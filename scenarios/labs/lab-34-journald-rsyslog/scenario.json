{
  "id": "lab-34-journald-rsyslog",
  "title": "Lab 34: Journald Persistence and Rsyslog",
  "description": "Configure persistent journal storage and a custom rsyslog drop-in for authentication warnings",
  "objective_tags": [
    "processes-logs-tuning",
    "software-scheduling-time"
  ],
  "supported_modes": [
    "lab"
  ],
  "time_limit_minutes": 25,
  "vm_scripts": {
    "clientvm": "scripts/clientvm.sh"
  },
  "flags": {
    "password_recovery": false,
    "requires_servervm": false
  },
  "content": {
    "lab": {
      "tasks": [
        "Configure journald on clientvm so logs are stored persistently across reboots.",
        "Create the drop-in file /etc/rsyslog.d/10-auth34.conf so authpriv messages with priority warning and higher are written to /var/log/auth34.log.",
        "Ensure the rsyslog service is active after your changes."
      ],
      "hints": [
        "Persistent journald storage requires the correct directory.",
        "Reload or restart the affected logging services after you update their configuration."
      ],
      "checks": [
        "test -d /var/log/journal && grep -Eq '^[[:space:]]*Storage[[:space:]]*=[[:space:]]*persistent[[:space:]]*$' /etc/systemd/journald.conf",
        "grep -Eq '^[[:space:]]*authpriv\\.warning[[:space:]]+/var/log/auth34\\.log[[:space:]]*$' /etc/rsyslog.d/10-auth34.conf",
        "systemctl is-active rsyslog | grep -qx active && rm -f /var/log/auth34.log && logger -p authpriv.warning 'lab34 probe' && sleep 2 && grep -Fq 'lab34 probe' /var/log/auth34.log"
      ],
      "solution_outline": [
        "Configure journald and rsyslog persistently."
      ],
      "task_titles": [
        "Configure journald on clientvm so logs are stored...",
        "Create the drop-in file...",
        "Ensure the rsyslog service is active after your..."
      ],
      "task_points": [
        10,
        10,
        10
      ],
      "solution_commands": [
        [
          "mkdir -p /var/log/journal",
          "vim /etc/systemd/journald.conf",
          "# Set: Storage=persistent",
          "systemctl restart systemd-journald"
        ],
        [
          "vim /etc/rsyslog.d/10-auth34.conf",
          "authpriv.warning    /var/log/auth34.log"
        ],
        [
          "systemctl restart rsyslog",
          "systemctl enable rsyslog"
        ]
      ]
    }
  }
}
