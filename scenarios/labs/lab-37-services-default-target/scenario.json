{
  "id": "lab-37-services-default-target",
  "title": "Lab 37: Services and Default Target",
  "description": "Manage the default target and key services on servervm.",
  "objective_tags": [
    "software-scheduling-time",
    "boot-and-recovery"
  ],
  "supported_modes": [
    "lab"
  ],
  "time_limit_minutes": 20,
  "vm_scripts": {
    "clientvm": "scripts/clientvm.sh",
    "servervm": "scripts/servervm.sh"
  },
  "flags": {
    "password_recovery": false,
    "requires_servervm": true
  },
  "content": {
    "lab": {
      "tasks": [
        "Configure servervm to boot into multi-user.target by default.",
        "Ensure the rsyslog service is enabled and running on servervm, and if postfix is installed disable it and stop it."
      ],
      "hints": [
        "This lab belongs on servervm, not clientvm.",
        "Treat postfix as conditional: only act if it is installed."
      ],
      "checks": [
        "ssh admin@servervm systemctl get-default | grep -qx multi-user.target",
        "ssh admin@servervm systemctl is-enabled rsyslog | grep -qx enabled && ssh admin@servervm systemctl is-active rsyslog | grep -qx active && ssh admin@servervm sh -lc 'rpm -q postfix >/dev/null 2>&1 || exit 0; systemctl is-enabled postfix 2>/dev/null | grep -qx disabled && systemctl is-active postfix 2>/dev/null | grep -qx inactive'"
      ],
      "solution_outline": [
        "Set the default target to multi-user.target.",
        "Enable and start rsyslog, then disable postfix only if the package is present."
      ],
      "task_titles": [
        "Set the default boot target on servervm",
        "Manage rsyslog and postfix on servervm"
      ],
      "task_points": [
        10,
        20
      ],
      "solution_commands": [
        [
          "systemctl set-default multi-user.target"
        ],
        [
          "systemctl enable --now rsyslog",
          "rpm -q postfix >/dev/null 2>&1 && systemctl disable --now postfix || true"
        ]
      ]
    }
  }
}
